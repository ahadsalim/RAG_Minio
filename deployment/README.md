# MinIO Server Deployment Scripts

این پوشه شامل اسکریپت‌های مدیریت و راه‌اندازی سرور MinIO با تمرکز بر امنیت است.

## 📋 فهرست اسکریپت‌ها

### 1. `start.sh` - راه‌اندازی امن سرور
اسکریپت اصلی برای شروع سرویس‌ها با بررسی‌های امنیتی کامل.

**ویژگی‌ها:**
- ✅ بررسی وضعیت Docker
- ✅ بررسی وجود فایل `.env`
- ✅ بررسی فعال بودن UFW firewall
- ✅ بررسی پیکربندی DOCKER-USER iptables chain
- ✅ راه‌اندازی خودکار docker-user-iptables service در صورت نیاز
- ✅ تأیید port binding به localhost
- ✅ نمایش وضعیت امنیتی کامل
- ✅ خروجی رنگی برای شناسایی آسان مشکلات

**استفاده:**
```bash
cd /srv/deployment
./start.sh
```

**پیش‌نیازها:**
- Docker و Docker Compose نصب شده باشد
- UFW firewall فعال باشد
- DOCKER-USER chain پیکربندی شده باشد
- فایل `.env` با متغیرهای لازم موجود باشد

---

### 2. `security-status.sh` - نمایش وضعیت امنیتی
اسکریپت بررسی و نمایش وضعیت امنیتی فعلی سرور بدون تغییر یا راه‌اندازی سرویس.

**ویژگی‌ها:**
- 🔍 نمایش وضعیت UFW firewall
- 🔍 نمایش قوانین DOCKER-USER chain
- 🔍 بررسی وضعیت docker-user-iptables service
- 🔍 بررسی port binding (localhost vs 0.0.0.0)
- 🔍 لیست کانتینرهای Docker
- 🔍 نمایش اینترفیس‌های شبکه
- 🔍 لیست تمام پورت‌های باز
- 💡 ارائه توصیه‌های امنیتی

**استفاده:**
```bash
cd /srv/deployment
./security-status.sh
```

**خروجی نمونه:**
```
═══════════════════════════════════════════════════════════════
  MinIO Server Security Status
═══════════════════════════════════════════════════════════════

▶ UFW Firewall
───────────────────────────────────────────────────────────────
✓ UFW is active
...

▶ DOCKER-USER iptables Chain
───────────────────────────────────────────────────────────────
✓ DOCKER-USER chain is configured (9 rules)
...
```

---

### 3. `backup_minio.sh` - پشتیبان‌گیری از MinIO
اسکریپت پشتیبان‌گیری خودکار از bucket‌های MinIO.

**استفاده:**
```bash
./backup_minio.sh --auto
```

---

### 4. `minio-init.sh` - مقداردهی اولیه MinIO
اسکریپت ساخت bucket و کاربر سرویس در MinIO (اجرای خودکار توسط docker-compose).

---

## 🔒 پیکربندی امنیتی

### فایل‌های امنیتی مهم:

1. **`/etc/ufw/after.rules`**
   - قوانین DOCKER-USER iptables
   - جلوگیری از bypass کردن UFW توسط Docker

2. **`/etc/systemd/system/docker-user-iptables.service`**
   - سرویس systemd برای پایداری قوانین iptables
   - اجرای خودکار بعد از restart Docker

3. **`docker-compose.yml`**
   - پورت‌های MinIO به `127.0.0.1` محدود شده
   - فقط از شبکه داخلی قابل دسترسی

### قوانین دسترسی:

| سرویس | پورت | دسترسی از اینترنت | دسترسی از LAN/DMZ |
|-------|------|-------------------|-------------------|
| SSH | 22 | ✅ بله | ✅ بله |
| HTTP | 80 | ✅ بله | ✅ بله |
| HTTPS | 443 | ✅ بله | ✅ بله |
| MinIO S3 API | 9000 | ❌ خیر | ✅ بله |
| MinIO Console | 9001 | ❌ خیر | ✅ بله |
| Node Exporter | 9100 | ❌ خیر | ✅ بله |
| cAdvisor | 8080 | ❌ خیر | ✅ بله |

### شبکه‌های مجاز:
- **LAN:** 192.168.100.0/24
- **DMZ:** 10.10.10.0/24
- **Docker Networks:** 172.16.0.0/12

---

## 🚀 راه‌اندازی اولیه

### مرحله ۱: ایجاد فایل `.env`
```bash
cd /srv/deployment
cat > .env << 'EOF'
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=your-strong-password-here
BUCKET_NAME=ingest-system
SERVICE_ACCESS_KEY=service-access-key
SERVICE_SECRET_KEY=service-secret-key
EOF

chmod 600 .env
```

### مرحله ۲: بررسی امنیت
```bash
./security-status.sh
```

### مرحله ۳: راه‌اندازی سرویس‌ها
```bash
./start.sh
```

---

## 🔧 دستورات مفید

### مشاهده لاگ‌ها:
```bash
cd /srv/deployment
docker compose logs -f minio
docker compose logs -f promtail
```

### توقف سرویس‌ها:
```bash
cd /srv/deployment
docker compose down
```

### Restart سرویس‌ها:
```bash
cd /srv/deployment
docker compose restart minio
```

### بررسی وضعیت کانتینرها:
```bash
docker compose ps
```

### دسترسی به MinIO از خارج (SSH Tunnel):
```bash
# از کامپیوتر خودتان:
ssh -L 9000:localhost:9000 -L 9001:localhost:9001 user@192.168.100.105

# سپس در مرورگر:
# http://localhost:9000  - S3 API
# http://localhost:9001  - Console
```

---

## 📊 Monitoring

### Prometheus Targets:
```yaml
- job_name: 'minio'
  static_configs:
    - targets: ['192.168.100.105:9000']
  metrics_path: /minio/v2/metrics/cluster

- job_name: 'node-exporter'
  static_configs:
    - targets: ['192.168.100.105:9100']

- job_name: 'cadvisor'
  static_configs:
    - targets: ['192.168.100.105:8080']
```

### Loki:
Promtail ارسال لاگ‌ها به `10.10.10.40:3100`

---

## ⚠️ نکات امنیتی مهم

1. **هرگز پورت‌های MinIO را به `0.0.0.0` bind نکنید**
   - همیشه از `127.0.0.1` استفاده کنید
   - برای دسترسی از خارج از SSH tunnel استفاده کنید

2. **DOCKER-USER chain را همیشه پیکربندی کنید**
   - UFW به تنهایی کافی نیست
   - Docker به طور پیش‌فرض UFW را bypass می‌کند

3. **قبل از هر تغییر در فایروال، SSH را تست کنید**
   - مطمئن شوید پورت 22 باز است
   - یک session SSH اضافی باز نگه دارید

4. **بعد از restart Docker، امنیت را بررسی کنید**
   - `./security-status.sh` را اجرا کنید
   - قوانین DOCKER-USER را تأیید کنید

5. **رمزهای قوی استفاده کنید**
   - حداقل 16 کاراکتر
   - ترکیبی از حروف، اعداد و علائم

---

## 🐛 عیب‌یابی

### مشکل: MinIO از اینترنت قابل دسترسی است
```bash
# بررسی port binding
ss -tlnp | grep -E ":(9000|9001)"

# باید فقط 127.0.0.1 نشان دهد، نه 0.0.0.0
# اگر 0.0.0.0 دیدید، docker-compose.yml را بررسی کنید
```

### مشکل: DOCKER-USER chain خالی است
```bash
# بررسی قوانین
sudo iptables -L DOCKER-USER -n -v

# اگر خالی بود:
sudo systemctl start docker-user-iptables.service
sudo systemctl enable docker-user-iptables.service
```

### مشکل: UFW غیرفعال است
```bash
# فعال‌سازی UFW
sudo ufw --force enable

# بررسی وضعیت
sudo ufw status verbose
```

### مشکل: کانتینر MinIO شروع نمی‌شود
```bash
# بررسی لاگ‌ها
docker compose logs minio

# بررسی .env file
cat .env

# بررسی permissions
ls -la /srv/deployment/.env
```

---

## 📚 مستندات بیشتر

- گزارش کامل بررسی امنیتی: `/srv/documents/security-audit-report.md`
- راهنمای راه‌اندازی Monitoring: `/srv/documents/monitoring-setup.md`

---

## 🔄 تاریخچه تغییرات

### 2026-02-17 - بررسی امنیتی کامل
- محدود کردن پورت‌های MinIO به localhost
- پیکربندی DOCKER-USER iptables chain
- ساخت docker-user-iptables.service
- افزودن بررسی‌های امنیتی به start.sh
- ساخت security-status.sh

### Git Commits:
- `576ef8d` - security: restrict MinIO ports to localhost only
- `8e190d5` - docs: add comprehensive security audit report
- `d7b7c28` - feat: enhance start.sh with comprehensive security checks
- `da3c8f5` - feat: add security-status.sh for comprehensive security monitoring

---

**نگهداری:** این سرور برای object storage MinIO با تمرکز بر امنیت پیکربندی شده است.  
**مسئول امنیت:** تمام تغییرات امنیتی باید مستند و در Git ثبت شوند.
